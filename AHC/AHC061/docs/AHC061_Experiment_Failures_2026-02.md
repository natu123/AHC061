# AHC061 Experiment Failures 2026-02

- 不採用実験のみを記録するログです。採用実験の詳細は `docs/AHC061_Experiment_Log_2026-02.md` を参照します。
- ナンバリングは採用/不採用を通した試行通番 `T-xxx` を使用します。
- `T-010..T-018` は改良9の前段で連続実施した不採用試行です。

## 2026-02-15 [T-010] F-01（future重みの全局面最適化）
- 背景:
  - `future` 重みを `phase` と `uncertainty` に連動させれば、中盤評価を底上げできると仮説を置きました。
- 変更:
  - `choose_move` で固定値 `0.18` を廃止し、`0.14..0.28` の範囲で可変化しました。
- 結果（seed 0..99）:
  - mean `141,675.4`
  - median `124,242`
  - min `34,603`
  - max `609,533`
- 比較（対 改良8）:
  - mean: `146,158.5 -> 141,675.4`（`-4,483.1`）
  - median: `124,500 -> 124,242`（`-258`）
  - min: `50,617 -> 34,603`（`-16,014`）
- 判定: 不採用
- タグ:
  - `future-weight-overfit`, `tail-risk-worsen`

## 2026-02-15 [T-011] F-02（future重みのM=5局所補正）
- 背景:
  - `M=5` で不確実性が高い局面のみ `future` を増やせば、平均を維持して改善できると判断しました。
- 変更:
  - `M=5` 条件で `future_weight=0.21`、終盤 `phase>=0.92` で `0.15` を設定しました。
- 結果（seed 0..99）:
  - mean `144,246.6`
  - median `119,762`
  - min `50,617`
  - max `605,548`
- 比較（対 改良8）:
  - mean: `146,158.5 -> 144,246.6`（`-1,911.9`）
  - median: `124,500 -> 119,762`（`-4,738`）
  - min: `50,617 -> 50,617`（`±0`）
- 判定: 不採用
- タグ:
  - `future-weight-local`, `median-drop`

## 2026-02-15 [T-012] F-03（M=4/6局面でbeam+1）
- 背景:
  - `M=5` 以外にも beam 拡張を広げれば全体が上がると仮説を置きました。
- 変更:
  - `M in {4,6}` かつ `phase<=0.75` かつ `uncertainty>=0.24` で `beam_width +1` を適用しました。
- 結果（seed 0..99）:
  - mean `144,346.6`
  - median `119,762`
  - min `50,617`
  - max `605,548`
- 比較（対 改良8）:
  - mean: `146,158.5 -> 144,346.6`（`-1,811.9`）
  - median: `124,500 -> 119,762`（`-4,738`）
  - min: `50,617 -> 50,617`（`±0`）
- 判定: 不採用
- タグ:
  - `beam-over`, `scope-expansion-fail`

## 2026-02-15 [T-013] F-04（leader攻撃補正の係数増量）
- 背景:
  - 改良6の補正を強めれば `M=5` 改善が伸びると判断しました。
- 変更:
  - `score += (0.10 + 0.20 * phase)` を `score += (0.12 + 0.24 * phase)` に変更しました。
- 結果（seed 0..99）:
  - mean `145,843.8`
  - median `121,564`
  - min `50,617`
  - max `605,548`
- 比較（対 改良7）:
  - mean/median/min/max はすべて同値（実質 no-op）
- 判定: 不採用
- タグ:
  - `no-op`, `parameter-insensitive`

## 2026-02-15 [T-014] F-05（beam拡張閾値緩和）
- 背景:
  - `uncertainty>=0.18` を `>=0.15` に緩めれば改善する想定でした。
- 変更:
  - 改良7の閾値のみ変更しました。
- 結果（seed 0..99）:
  - mean `145,843.8`
  - median `121,564`
  - min `50,617`
  - max `605,548`
- 比較（対 改良7）:
  - mean/median/min/max はすべて同値（実質 no-op）
- 判定: 不採用
- タグ:
  - `no-op`, `threshold-insensitive`

## 2026-02-15 [T-015] F-06（M=5局面でbeam+4）
- 背景:
  - 改良8の `beam +3` を `+4` にすれば改善が伸びると仮定しました。
- 変更:
  - 条件成立時の beam 拡張量を `+3` から `+4` に変更しました。
- 結果（seed 0..99）:
  - mean `146,158.5`
  - median `124,500`
  - min `50,617`
  - max `605,548`
  - elapsed `12,809ms`
- 比較（対 改良8）:
  - mean/median/min/max はすべて同値（実質 no-op）
  - 実行時間のみ悪化
- 判定: 不採用
- タグ:
  - `no-op`, `beam-insensitive`

## 2026-02-15 [T-016] F-07（M=5条件のphase閾値を0.95へ緩和）
- 背景:
  - `phase<=0.85` を `<=0.95` に緩めれば中後半の改善が見込めると考えました。
- 変更:
  - 改良8の phase 閾値のみ変更しました。
- 結果（seed 0..99）:
  - mean `146,158.5`
  - median `124,500`
  - min `50,617`
  - max `605,548`
- 比較（対 改良8）:
  - mean/median/min/max はすべて同値（実質 no-op）
- 判定: 不採用
- タグ:
  - `no-op`, `threshold-insensitive`

## 2026-02-15 [T-017] F-08（local_score係数を0.10へ調整）
- 背景:
  - `rollout` 重視を強めれば平均が上がる仮説でした。
- 変更:
  - `rollout + 0.12 * local_score` を `rollout + 0.10 * local_score` に変更しました。
- 結果（seed 0..99）:
  - mean `143,929.2`
  - median `119,762`
  - min `53,012`
  - max `605,548`
- 比較（対 改良8）:
  - mean: `146,158.5 -> 143,929.2`（`-2,229.3`）
  - median: `124,500 -> 119,762`（`-4,738`）
  - min: `50,617 -> 53,012`（`+2,395`）
- 判定: 不採用
- タグ:
  - `weight-tuning-fail`, `median-drop`

## 2026-02-15 [T-018] F-09（M=3序盤未強化ボーナス）
- 背景:
  - `M=3` 向けに序盤未強化セルへボーナスを追加すれば、`M=3` が改善すると仮定しました。
- 変更:
  - `evaluate_local_move` の未強化評価に `m3_focus` ボーナスを追加しました。
- 結果（seed 0..99）:
  - mean `145,064.1`
  - median `121,564`
  - min `50,617`
  - max `605,548`
  - `M=3` mean `121,629.4`
- 比較（対 改良8）:
  - mean: `146,158.5 -> 145,064.1`（`-1,094.4`）
  - `M=3` mean: `129,446.4 -> 121,629.4`（`-7,817.0`）
- 判定: 不採用
- タグ:
  - `targeted-bonus-fail`, `group-regression`

## 運用ルール
- 不採用試行も本ファイルへ必ず記録します。
- 記録粒度は `背景 / 変更 / 結果 / 比較 / 判定 / タグ` を最小セットとします。
- 採用ログへの詳細転記が必要な場合は、`docs/AHC061_Experiment_Log_2026-02.md` から本ファイルを参照します。