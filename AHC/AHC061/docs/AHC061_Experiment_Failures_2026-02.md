# AHC061 Experiment Failures 2026-02

## 目的
- 不採用となった実験を簡潔に記録し、同種の失敗を繰り返さないための参照台帳とします。
- 採用ログは `docs/AHC061_Experiment_Log_2026-02.md` を正本とし、本書は不採用詳細の補助記録とします。
- ナンバリングは採用/不採用を通した試行通番 `T-xxx` を使用します。

## 2026-02-15 [T-010] F-01（future重みの全局面依存化）
- 仮説:
  - `future` 重みを `phase` と `uncertainty` に連動させれば、中央値を押し上げられる。
- 変更:
  - `choose_move` で固定 `0.18` を動的重み（`0.14..0.28`）へ変更。
- 結果（`seed 0..99`）:
  - mean `141,675.4`
  - median `124,242`
  - min `34,603`
  - max `609,533`
- 比較（対 改良8）:
  - mean: `146,158.5 -> 141,675.4`（`-4,483.1`）
  - median: `124,500 -> 124,242`（`-258`）
  - min: `50,617 -> 34,603`（`-16,014`）
- 判定:
  - 不採用（下振れ悪化が大きすぎる）
- タグ:
  - `future-weight-overfit`, `tail-risk-worsen`

## 2026-02-15 [T-011] F-02（future重みのM=5限定補正）
- 仮説:
  - `M=5` 高不確実局面だけ `future` 重みを増やせば、平均を維持したまま底上げできる。
- 変更:
  - `M=5` 条件で `future_weight=0.21`、終盤 `phase>=0.92` で `0.15` 上限を設定。
- 結果（`seed 0..99`）:
  - mean `144,246.6`
  - median `119,762`
  - min `50,617`
  - max `605,548`
- 比較（対 改良8）:
  - mean: `146,158.5 -> 144,246.6`（`-1,911.9`）
  - median: `124,500 -> 119,762`（`-4,738`）
  - min: `50,617 -> 50,617`（`±0`）
- 判定:
  - 不採用（平均・中央値とも悪化）
- タグ:
  - `future-weight-local`, `median-drop`

## 2026-02-15 [T-012] F-03（M=4/6高不確実局面でbeam+1）
- 仮説:
  - 改良8の `M=5` 拡張に加えて `M=4/6` でも軽く beam を増やせば改善域が広がる。
- 変更:
  - `M in {4,6}` かつ `phase<=0.75` かつ `uncertainty>=0.24` で `beam_width +1`。
- 結果（`seed 0..99`）:
  - mean `144,346.6`
  - median `119,762`
  - min `50,617`
  - max `605,548`
- 比較（対 改良8）:
  - mean: `146,158.5 -> 144,346.6`（`-1,811.9`）
  - median: `124,500 -> 119,762`（`-4,738`）
  - min: `50,617 -> 50,617`（`±0`）
- 判定:
  - 不採用（改善域を広げる意図に反して全体が悪化）
- タグ:
  - `beam-over`, `scope-expansion-fail`

## 2026-02-15 [T-013] F-04（leader攻撃補正の係数微増）
- 仮説:
  - 改良6の係数をわずかに増やせば `M=5` の追加改善が得られる。
- 変更:
  - `score += (0.10 + 0.20 * phase)` を `score += (0.12 + 0.24 * phase)` に変更。
- 結果（`seed 0..99`）:
  - mean `145,843.8`
  - median `121,564`
  - min `50,617`
  - max `605,548`
- 比較（対 改良7）:
  - mean/median/min/max すべて同一（実質 no-op）
- 判定:
  - 不採用（挙動変化なし）
- タグ:
  - `no-op`, `parameter-insensitive`

## 2026-02-15 [T-014] F-05（beam拡張条件の閾値緩和）
- 仮説:
  - `uncertainty>=0.18` を `>=0.15` に緩めれば追加改善が得られる。
- 変更:
  - 改良7の条件閾値のみ変更。
- 結果（`seed 0..99`）:
  - mean `145,843.8`
  - median `121,564`
  - min `50,617`
  - max `605,548`
- 比較（対 改良7）:
  - mean/median/min/max すべて同一（実質 no-op）
- 判定:
  - 不採用（挙動変化なし）
- タグ:
  - `no-op`, `threshold-insensitive`

## 2026-02-15 [T-015] F-06（M=5高不確実局面でbeam+4）
- 仮説:
  - 改良8の `beam +3` を `+4` に増やせば追加改善が得られる。
- 変更:
  - 条件付き beam 拡張量を `+3` から `+4` に変更。
- 結果（`seed 0..99`）:
  - mean `146,158.5`
  - median `124,500`
  - min `50,617`
  - max `605,548`
  - elapsed `12,809ms`
- 比較（対 改良8）:
  - mean/median/min/max すべて同一（実質 no-op）
  - 実行時間のみ微増
- 判定:
  - 不採用（効果なし）
- タグ:
  - `no-op`, `beam-insensitive`

## 2026-02-15 [T-016] F-07（M=5条件のphase閾値を0.95へ緩和）
- 仮説:
  - `phase<=0.85` を `<=0.95` に緩めれば、中終盤で追加改善が得られる。
- 変更:
  - 改良8の条件で phase 閾値のみ変更。
- 結果（`seed 0..99`）:
  - mean `146,158.5`
  - median `124,500`
  - min `50,617`
  - max `605,548`
- 比較（対 改良8）:
  - mean/median/min/max すべて同一（実質 no-op）
- 判定:
  - 不採用（効果なし）
- タグ:
  - `no-op`, `threshold-insensitive`

## 2026-02-15 [T-017] F-08（local_score混合係数を0.10へ低減）
- 仮説:
  - `rollout` 重視を強めれば平均が上がる。
- 変更:
  - `rollout + 0.12 * local_score` を `rollout + 0.10 * local_score` に変更。
- 結果（`seed 0..99`）:
  - mean `143,929.2`
  - median `119,762`
  - min `53,012`
  - max `605,548`
- 比較（対 改良8）:
  - mean: `146,158.5 -> 143,929.2`（`-2,229.3`）
  - median: `124,500 -> 119,762`（`-4,738`）
  - min: `50,617 -> 53,012`（`+2,395`）
- 判定:
  - 不採用（平均・中央値の悪化が大きい）
- タグ:
  - `weight-tuning-fail`, `median-drop`

## 2026-02-15 [T-018] F-09（M=3序盤未領土取得ボーナス）
- 仮説:
  - `M=3` 専用に序盤の未領土取得ボーナスを追加すれば、`M=3` 帯が改善する。
- 変更:
  - `evaluate_local_move` の未領土評価に `m3_focus` 依存ボーナスを追加。
- 結果（`seed 0..99`）:
  - mean `145,064.1`
  - median `121,564`
  - min `50,617`
  - max `605,548`
  - `M=3` mean `121,629.4`
- 比較（対 改良8）:
  - mean: `146,158.5 -> 145,064.1`（`-1,094.4`）
  - `M=3` mean: `129,446.4 -> 121,629.4`（`-7,817.0`）
- 判定:
  - 不採用（狙いの `M=3` が悪化）
- タグ:
  - `targeted-bonus-fail`, `group-regression`

## 運用ルール
- 不採用実験は本ファイルへ必ず記録します。
- 記録粒度は `仮説 / 変更 / 結果 / 比較 / 判定 / タグ` を最小セットとします。
- 採用ログへの詳細追記は行わず、`AHC061_Experiment_Log_2026-02.md` から本ファイルを参照します。
