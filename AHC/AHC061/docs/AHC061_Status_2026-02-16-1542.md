# AHC061 Status Snapshot 2026-02-16-1542

## 目的
- 40分後にネット移動があり、改善Loopを安全に中断・再開するための状態引継ぎ用スナップショットです。

## 現在の採用ベース
- ベース名: `x04`（現行共通ベース）
- 探索中ポリシー: 長期Gear-Shift運用（`mode_weight` を `0.85` で `QualityFirst` 優先）
- 直近未採用の有望系統:
  - `x27`, `x29`, `x42`（full seed 0..99 まで検証済み、再現性確認待ち）

## 直近実行結果（要点）
- `docs/AHC061_Experiment_Log_2026-02.md` 更新済み（最新：x27/x29/x42のquick/full比較）
- `docs/AHC061_Experiment_Failures_2026-02.md` で `F-79`（`qualityfirst timeout interrupt`）を残して停止状態を明示済み
- `tmp_eval_loop10.ps1` は以下のように `full`の候補上位競合判定に対応済み
  - `-CandidateIds x42,x47,x48,x49`
  - `-QuickSeedTo 19`
  - `-FullSeedTo 99`
  - `-FullCandidateLimit 1`

## 進捗上の注意
- `F-79` は `tmp_eval_loop10.ps1` 本体がfull実行時間制約で中断した不採用扱いです。  
  残タスクは再設計ではなく「再開時に再評価」を優先してください。

## 再開手順（インターネット復帰後）
1. ワークツリー確認
   - `git status --short`
2. 直近状態再確認
   - `docs/AHC061_Status_2026-02-16-1542.md`
   - `docs/AHC061_Experiment_Log_2026-02.md`
   - `docs/AHC061_Experiment_Failures_2026-02.md`
3. 再開コマンド（保守）
   - `powershell -NoProfile -ExecutionPolicy Bypass -File .\tmp_eval_loop10.ps1 -Mode QualityFirst -LoopCount 1 -QuickSeedTo 19 -FullSeedTo 99 -FullCandidateLimit 1 -CandidateIds x42`
4. quick fullの閾値に対して有望なら `-CandidateIds` を再拡張して再開

## 次アクション（40分後）
- 再開し、`x42` のquick→full再実施で比較結果を確定
- 必要なら `F-` 不採用ログ追加と、新規採用候補の `built` 反映
